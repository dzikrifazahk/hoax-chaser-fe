<div class="form-group">
    <label for="{{$title}}">{{$title}}</label>
    <input type="date" class="form-control" id="{{$title}}" value="{{$value}}" name="{{$name}}" placeholder="Enter {{$title}}">
</div>