<div class="form-group">
    <label for="{{$title}}">{{$title}}</label>
    <input type="text" class="form-control" value="{{$value}}" id="{{$title}}" name="{{$name}}" placeholder="Enter {{$title}}">
</div>